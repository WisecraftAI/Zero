import styles from './Footer.module.scss';

export function Footer() {
  return (
    <footer className={styles.footer}>
      ZER0 · V3 Architecture Docs · React 19 + Vite 6 + TypeScript strict ·
      updated from code
    </footer>
  );
}
