---
name: zero-locators
description: >-
  Code ZER0 locator merge (packages/locators). Use when the user
  asks to update locators, selectors, element keys, or @zero/locators.
---

# @zero/locators

1. Read `agent-workflow/prompts/repos/locators.md`
2. Merge order is profile → memory → DB — do not change it
3. Normalize keys only via `packages/locators/elementLogger.js`
